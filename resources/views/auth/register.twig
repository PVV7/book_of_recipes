

{% block title %}Регистрация{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css') }}"integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
{% endblock %}

{% block body %}
    <div class="container">
        <div class="login row justify-content-md-center m-0 pt-3">
            <div class="col-lg-4 shadow p-3 mt-5 bg-body rounded">
{#                <form action="{{ route('register') }}" method="POST" id="form-login" class="needs-validation">#}
                <form action="{{ route('register') }}" method="POST" id="form-login">
                    <input type="hidden" name="_token" value="{{ csrf_token() }}">
                    {{ errors }}
{#                        <ul>#}
{#                            {% for key, error in errors %}#}
{#                                <li>#}
{#                                    {{ key }}#}
{#                                    {{ error }}#}
{#                                </li>#}
{#                            {% endfor %}#}
{#                        </ul>#}
                    <div class="mb-3">
					    <label for="userName" class="form-label">
                            Имя
                        </label>
{#                            {% if errors.has('name') %}#}
{#                                <p class="error">{{ errors.first('name') }}</p>#}
{#                            {% endif %}#}
                        <input type="text" class="form-control" name="name" id="name" aria-describedby="userNameHelp">
                        <div class="invalid-feedback">
                            Write a valid user name
                        </div>
                    </div>

                    <div class="mb-3">
					    <label for="email" class="form-label">
                            E-mail
                        </label>
                        <input type="text" class="form-control" name="email" id="email" aria-describedby="userNameHelp">
                        <div class="invalid-feedback">
                            Write a valid user name
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="pass" class="form-label">
                            Пароль
                        </label>
                        <input type="text" class="form-control" name="password" id="pass" aria-describedby="passHelp">
                        <div class="invalid-feedback">
                            Write your password
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="confirmPass" class="form-label">
                            Подтверждение пароля
                        </label>
                        <input type="text" class="form-control" name="password_confirmation" id="confirmPass" aria-describedby="passHelp">
                        <div class="invalid-feedback">
                            Write your password
                        </div>
                    </div>

                    <button type="submit" class="w-full btn btn-primary btn-acc-group">Отправить</button>
                </form>
            </div>
        </div>
    </div>>
{% endblock %}

