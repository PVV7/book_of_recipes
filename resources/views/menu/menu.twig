{% block stylesheets %}
    <style>
            nav {
      margin: 27px auto 0;

      position: relative;
      width: 590px;
      height: 50px;
      background-color: #252525;
      border-radius: 8px;
      font-size: 0;
    }
    nav a {
      line-height: 50px;
      height: 100%;
      font-size: 15px;
      display: inline-block;
      position: relative;
      z-index: 1;
      text-decoration: none;
      text-transform: uppercase;
      text-align: center;
      color: white;
      cursor: pointer;
    }


    a:nth-child(1) {
      width: 100px;
    }
    a:nth-child(2) {
      width: 110px;
    }
    a:nth-child(3) {
      width: 100px;
    }
    a:nth-child(4) {
      width: 160px;
    }
    a:nth-child(5) {
      width: 120px;
    }


    </style>

{% endblock %}

{% block body %}
{#    добавить директиву guest#}
{#    добавить директиву auth (контент для аутентифицированных пользователей)#}
    <nav>
      <a href="{{ route('dashboard') }}">Home</a>
      <a href="{{ route('login') }}">Login</a>
      <a href="{{ route('register') }}">Register</a>
{#      <a href="#">Portefolio</a>#}

        реализовать выход при помощи post метода
        <form method="post" action="{{ route('logout') }}">
            <input type="hidden" name="_token" value="{{ csrf_token() }}">
            <button type="submit" style="width: 100px; height: 50px" value="Logout"></button>
        </form>

      <div class="animation start-home"></div>
    </nav>

    {% block content %}
    {% endblock %}

{% endblock %}

